# Server Management for Load Balancer Logs

---

### **Directory Creation & Organization:**

1. Create a directory named "load_balancer_logs".
2. Inside "load_balancer_logs", create 12 subdirectories, each named after a month (e.g., "January", "February", etc.).
3. Inside each month’s directory, create log files that track the load balancing statistics for each day (e.g., "load_balancer_log_day_1.txt", "traffic_data_day_1.csv").

---

### **Move and Rename Files:**

1. Move a log file (e.g., "load_balancer_log_day_1.txt") from one month's directory to another (e.g., from "January" to "February").
2. Rename a file within a directory (e.g., rename "traffic_data_day_1.csv" to "traffic_data_day_1_updated.csv") and verify the change by listing the contents of the directory.

---

### **Navigation & Listing Files:**

1. Navigate to the "load_balancer_logs" directory.
2. List the contents of the directory and verify the presence of all month directories (e.g., "January", "February", etc.) and the log files created in the previous task.

---

### **File Permissions Management:**

1. Create a file named "load_balancer_config.txt" in the "load_balancer_logs" directory.
2. Change the permissions of the file so that only the user has read, write, and execute permissions.
3. Verify the permission changes by listing the file's details.

---

### **Backup Files:**

1. Create a backup of a log file (e.g., "load_balancer_log_day_1.txt") by copying it to another directory (e.g., "backup").
2. Verify the copy operation by listing the contents of the backup directory.

---

### **Removing Files & Directories:**

1. Delete one of the log files in the "load_balancer_logs" directory (e.g., delete "traffic_data_day_1.csv").
2. Remove an empty subdirectory from one of the months' directories (e.g., remove the "March" directory if it's empty).

---

### **Creating a Script for File Generation:**

1. Write a script that creates 100 log files inside the "load_balancer_logs" directory, with each file representing unique load balancing statistics (e.g., "load_balancer_log_1.txt", "traffic_data_2.csv").

---

### **Exploring File History:**

1. View the command history to see the last 20 commands executed.
2. Search the history for any command related to load balancing logs or file manipulation.

---

### **System Monitoring:**

1. Check the system's uptime to ensure the load balancer server is running without interruption.
2. View the system's load and resource usage statistics to verify the load balancing server can handle high traffic.

---

### **Checking File Ownership and Permissions:**

1. Check the ownership and group of the "load_balancer_config.txt" file and verify whether they are correct for managing the configuration files of the load balancer.

---

### **Ping Test & Network Verification:**

1. Verify network connectivity by pinging the backend servers or a remote service the load balancer is balancing traffic for.
2. Record the response times and verify the connectivity between the load balancer and the backend servers.

---

### **Search for Specific Files or Content:**

1. Search for a specific log file within the "load_balancer_logs" directory by its name (e.g., search for "load_balancer_log_day_15.txt").
2. Search for a specific string of text (e.g., "server failure") inside one of the log files in the directory.

---

### **Create a Directory for Each Load Balancer Instance:**

1. Create a directory for each load balancer instance (e.g., "lb_instance_1", "lb_instance_2") and assign appropriate permissions to store configuration files and logs for each instance.

---

### **Creating a Script for Directory Cleanup:**

1. Write a script that deletes all empty subdirectories from the "load_balancer_logs" directory to keep the file structure organized and ensure that unused directories are removed.

---

### **File Sorting & Management:**

1. Sort log files in the "load_balancer_logs" directory by size and then list the files based on their size to help in identifying large log files.
2. Create a report listing the largest and smallest log files in the directory to identify which logs are taking up more space.

---

### **File Type Identification:**

1. Identify and list log files of a specific type (e.g., .txt, .csv) used for storing load balancing data and traffic reports in the "load_balancer_logs" directory.

---

### **File Compression and Archive:**

1. Compress the "load_balancer_logs" directory into a single archive file for backup purposes.
2. Verify the contents of the archive without extracting it to ensure it contains all necessary log files and data reports.

---

### NOTE: Finally fetch all the project commands from history and create a detailed project report "your_name_rollname.md" and push it to git your git repository